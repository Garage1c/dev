
&НаСервере
Процедура ОтправитьНаСервере()
	
	Справочники.СообщенияПользователям.СформироватьСообщенияОбИзмененияхЛимитов();
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	текдата = ТекущаяДата();
	
	ОтправитьНаСервере();
	
	ПоказатьОповещениеПользователя("Завершение",,"Процедура отправки выполнена " + Строка(ТекущаяДата() - текдата) + " сек.");
	
КонецПроцедуры
