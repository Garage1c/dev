
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если ЗначениеЗаполнено(Параметры.ОтборАдресата) Тогда
		ОтборыСписковКлиентСервер.УстановитьЭлементОтбораСписка(Список, "Адресация.Адресат", Параметры.ОтборАдресата);
		АвтоЗаголовок = Ложь;
		Заголовок = "Сообщения: " + Параметры.ОтборАдресата; КонецЕсли;
	
КонецПроцедуры
