
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Объект.Ссылка.Пустая() Тогда // Забъем по умолчанию
		
		Объект.АдресСервераSMTP 	= "smtp.gmail.com";
		Объект.ПортSMTP 			= 587; 
		
		Объект.Шаблон = "Добрый день,
		|
		|
		|----
		|С уважением, " + ПараметрыСеанса.ТекущийПользователь; КонецЕсли;
	
КонецПроцедуры
