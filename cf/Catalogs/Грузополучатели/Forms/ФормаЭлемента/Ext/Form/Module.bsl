&НаКлиенте
Процедура ТекстКонтактнойИнформацииНажатие(Элемент)
	
	Если Объект.Ссылка.Пустая() Тогда
		ПоказатьПредупреждение(,"Необходимо сперва сохранить грузополучателя");
	Иначе
		ОткрытьФорму("Обработка.КонтактнаяИнформация.Форма.ФормаГлавная", Новый Структура("Контакт, ЗакрыватьПриЗакрытииВладельца", Объект.Ссылка, Истина), ЭтаФорма, УникальныйИдентификатор);
	КонецЕсли;
	
КонецПроцедуры
