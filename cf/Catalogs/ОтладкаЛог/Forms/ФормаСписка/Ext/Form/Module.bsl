
&НаСервере
Процедура ОчиститьНаСервере()
	НачатьТранзакцию();
	Выборка=Справочники.ОтладкаЛог.Выбрать();
	Пока Выборка.Следующий() Цикл
		Об=Выборка.ПолучитьОбъект();
		Об.Удалить();
	КонецЦикла;
	ЗафиксироватьТранзакцию();
КонецПроцедуры

&НаКлиенте
Процедура Очистить(Команда)
	ОчиститьНаСервере();
КонецПроцедуры
