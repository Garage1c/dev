
&НаСервере
Функция ПолучитьТоварыИзЗапроса()
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Для Каждого Строка Из текПараметры Цикл Запрос.УстановитьПараметр(Строка.Параметр, Строка.Значение) КонецЦикла;
		
	Возврат ПоместитьВоВременноеХранилище(Запрос.Выполнить().Выгрузить());
	
КонецФункции

&НаКлиенте
Процедура ВыполнитьИВставитьВКорзину(Команда)
	
	Закрыть(ПолучитьТоварыИзЗапроса());
	
КонецПроцедуры
