
&НаСервере
Функция ОбновитьСтатусы()
	
	Возврат РегистрыСведений.СрезЗадачПоДням.ОбновитьСтатусыВсехЗадач();
	
КонецФункции
&НаКлиенте
Процедура ПользовательРешил(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ = КодВозвратаДиалога.Да Тогда
		
		текДата = ТекущаяДата();
		Если ОбновитьСтатусы() Тогда
			ОбщиеФункции.СообщитьТекст("Обновление заверешено сек: " + Строка(ТекущаяДата() - текДата)); КонецЕсли; КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ПоказатьВопрос(Новый ОписаниеОповещения("ПользовательРешил", ЭтотОбъект),"Переписать всю таблицу статусов?", РежимДиалогаВопрос.ДаНет,,,"Серъезный вопрос");
	
КонецПроцедуры
