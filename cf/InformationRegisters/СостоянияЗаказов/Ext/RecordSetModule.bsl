
Процедура ПриЗаписи(Отказ, Замещение)
	
	Если Отбор.Количество() Тогда
		Для Каждого Строка Из ЭтотОбъект Цикл
			
			НовЗапись = РегистрыСведений.СостоянияЗаказовТекущее.СоздатьМенеджерЗаписи();
			
			НовЗапись.Заказ 		= Строка.Заказ;
			НовЗапись.Состояние 	= Строка.Состояние;
			НовЗапись.ДатаСобытия 	= Строка.Период;
			
			Если Не ОбщиеФункции.ЗаписатьОбъектИСообщитьЕслиОшибка(НовЗапись) Тогда Отказ = Истина КонецЕсли; КонецЦикла; КонецЕсли;
	
КонецПроцедуры
