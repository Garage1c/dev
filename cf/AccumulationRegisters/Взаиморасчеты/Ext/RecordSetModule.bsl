
Процедура ПередЗаписью(Отказ, Замещение)
	
	// удалим строки с нулевой суммой
	времТаблица = Выгрузить();
	Строки = времТаблица.НайтиСтроки(Новый Структура("Сумма", 0));
	
	Если Строки.Количество() > 0 Тогда
		кол = Строки.Количество();
		Для ид = кол По 1 Цикл	времТаблица.Удалить(Строки[ид-1]); КонецЦикла; 
		
		Загрузить(времТаблица); КонецЕсли;

КонецПроцедуры
