
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Строка = "";
	Если ВвестиСтроку(Строка, "Введите штрих код:", 13) Тогда
		
		Оповестить("ПолученШтрихкод", СокрЛП(Строка), "ИмитаторШтрихКода");
		
	КонецЕсли;
	
КонецПроцедуры
