
&НаКлиенте
Процедура ОтправитьОтвет(Команда)
	Вложения 		= Новый Массив;
	Кому			= Новый Массив;
	Если НЕ ПустаяСтрока(Объект.Почта) Тогда
		Кому.Добавить(Новый Структура("Почта", Объект.Почта));
	КонецЕсли;
	
	// Откроем форму
	ОткрытьФорму("Документ.Письмо.Форма.Письмо2", 
			Новый Структура("Кому, Вложения, УдалитьФайлыПослеОтправки", Кому, Вложения, Истина), 
			ЭтаФорма,
			ЭтаФорма.УникальныйИдентификатор, 
			, Объект.Ссылка,, РежимОткрытияОкнаФормы.Независимый);
КонецПроцедуры
