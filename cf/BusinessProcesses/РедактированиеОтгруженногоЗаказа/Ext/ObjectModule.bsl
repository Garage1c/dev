
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	
	тип = ТипЗнч(ДанныеЗаполнения);
	
	Если Тип = Тип("БизнесПроцессСсылка.ЗаявкаПокупателя") ИЛИ Тип = Тип("БизнесПроцессСсылка.ИнтернетЗаявка") Тогда
		
		Заказ = ДанныеЗаполнения.Заказ;
		
	ИначеЕсли Тип = Тип("ДокументОбъект.ЗаказПокупателя") ИЛИ Тип = Тип("ДокументОбъект.ИнтернетЗаказПокупателя") Тогда
		
		Заказ = ДанныеЗаполнения; КонецЕсли;
	
КонецПроцедуры
