
Процедура ИнформацияОДолгеНачало(ИнфСтр1, ИнфСтр2, ИнфСтр3) Экспорт
	
	ИнфСтр1 = Новый ФорматированнаяСтрока("");
	ИнфСтр2 = Новый ФорматированнаяСтрока("Получение долга ...");
	ИнфСтр3 = Новый ФорматированнаяСтрока("");
	
КонецПроцедуры

Процедура ОбработатьНавигационнуюСсылку(НавигационнаяСсылка, СтандартнаяОбработка, ЭтаФорма) Экспорт
	
	Если Лев(НавигационнаяСсылка, 16) = "ЗначениеВСтроке:" Тогда
		
		СтандартнаяОбработка = Ложь;
		Структура = CRM.ПолучитьСтруктуруИзСтроки(Сред(НавигационнаяСсылка, 17));
		
		Если Структура.Действие = "Открыть" Тогда
			ОткрытьФорму(Структура.Форма, Структура.Параметры, ЭтаФорма); КонецЕсли; КонецЕсли;
	
КонецПроцедуры