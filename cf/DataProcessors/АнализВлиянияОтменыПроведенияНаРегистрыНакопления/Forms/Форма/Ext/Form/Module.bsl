
&НаСервере
Процедура ЗаполнитьНаСервере()
	
	Обработка = РеквизитФормыВЗначение("Объект");
	Если Обработка.ЗаполнитьЗависимости() Тогда
		
		Объект.ЗависимыеДокументы.Загрузить(Обработка.ЗависимыеДокументы.Выгрузить());
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Заполнить(Команда)
	
	ЗаполнитьНаСервере();
	
КонецПроцедуры
